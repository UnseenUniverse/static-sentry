# static-sentry
StaticSentry is a Python-based cybersecurity scanning tool with a modern GUI built using CustomTkinter. It performs static analysis on files to detect sensitive data and suspicious patterns.
